version: '3'

vars:
  IMAGE: omnivoicetts:test
  TINY_IMAGE: omnivoicetts:tiny
  PORT: '7861'
  GPU_DEVICE: '1'
  APP_DEVICE: 'auto'
  HF_TOKEN: ''
  HF_CACHE_VOLUME: omnivoicetts_hf_cache
  HF_HUB_OFFLINE: '1'
  TRANSFORMERS_OFFLINE: '1'
  CONTAINER: omnivoicetts
  CONTAINER_LOCAL: omnivoicetts_local
  TEMP_AUDIO: '%TEMP%\\omnivoicetts_task_audio'
  API_TEXT: 'Hello world. This is OmniVoiceTTS.'
  API_LANGUAGE: 'English'
  API_VOICE: 'af_heart'
  API_FORMAT: 'mp3'
  DRY_RUN: '0'
  NEXT_VERSION: ''
  SKIP_VALIDATION: '0'
  DEVPI_URL: 'http://192.168.10.148:3141/root/pypi/+simple/'
  DEVPI_TRUSTED_HOST: '192.168.10.148'
  LOCAL_EXTRA_INDEX_URLS: 'https://pypi.org/simple/ https://download.pytorch.org/whl/cu128'
  DEVPI_TEST_PACKAGE: 'simplejson'
  NAS_HOST: '192.168.10.148'
  NAS_SSH_PORT: '1022'
  NAS_USER: 'admin'

tasks:
  gpuinfo:
    desc: Show host GPU indexes
    cmds:
      - cmd /c nvidia-smi --query-gpu=index,name --format=csv,noheader

  devpi-check:
    desc: Check the local LAN devpi mirror used by local Docker builds
    cmds:
      - cmd /c curl.exe -sS -I {{.DEVPI_URL}}

  devpi-package-check:
    desc: Verify the LAN devpi mirror can resolve and download a real package
    cmds:
      - cmd /c if exist "%TEMP%\omnivoicetts-devpi-check" rmdir /s /q "%TEMP%\omnivoicetts-devpi-check"
      - cmd /c mkdir "%TEMP%\omnivoicetts-devpi-check"
      - cmd /c python -m pip download --no-deps --index-url {{.DEVPI_URL}} --trusted-host {{.DEVPI_TRUSTED_HOST}} --dest "%TEMP%\omnivoicetts-devpi-check" {{.DEVPI_TEST_PACKAGE}}

  nas-shell:
    desc: Open SSH shell to the Synology NAS
    cmds:
      - ssh -p {{.NAS_SSH_PORT}} {{.NAS_USER}}@{{.NAS_HOST}}

  image:
    desc: Build local baked OmniVoiceTTS Docker image using LAN devpi
    cmds:
      - cmd /c docker build --target baked -t {{.IMAGE}} --build-arg PIP_INDEX_URL={{.DEVPI_URL}} --build-arg "PIP_EXTRA_INDEX_URL={{.LOCAL_EXTRA_INDEX_URLS}}" --build-arg PIP_TRUSTED_HOST={{.DEVPI_TRUSTED_HOST}} .

  image-tiny:
    desc: Build local tiny OmniVoiceTTS Docker image without baked model cache using LAN devpi
    cmds:
      - cmd /c docker build --target tiny -t {{.TINY_IMAGE}} --build-arg PIP_INDEX_URL={{.DEVPI_URL}} --build-arg "PIP_EXTRA_INDEX_URL={{.LOCAL_EXTRA_INDEX_URLS}}" --build-arg PIP_TRUSTED_HOST={{.DEVPI_TRUSTED_HOST}} .

  image-public:
    desc: Build local baked image with public package indexes, matching GitHub Actions
    cmds:
      - cmd /c docker build --target baked -t {{.IMAGE}} .

  image-tiny-public:
    desc: Build local tiny image with public package indexes, matching GitHub Actions
    cmds:
      - cmd /c docker build --target tiny -t {{.TINY_IMAGE}} .

  cache-init:
    desc: Seed the persistent Hugging Face cache volume from the baked image
    cmds:
      - cmd /c docker volume create {{.HF_CACHE_VOLUME}} >nul
      - cmd /c docker run --rm -v {{.HF_CACHE_VOLUME}}:/cache {{.IMAGE}} sh -c "mkdir -p /cache && cp -an /app/.cache/huggingface/. /cache/ || true"

  release:
    desc: Release current VERSION snapshot, tag it, and prepare next snapshot
    cmds:
      - powershell -NoProfile -ExecutionPolicy Bypass -File scripts/release.ps1 -DryRun "{{.DRY_RUN}}" -NextVersion "{{.NEXT_VERSION}}" -SkipValidation "{{.SKIP_VALIDATION}}"

  deps:
    desc: Regenerate Linux/Python 3.11 requirements.txt from requirements.in
    cmds:
      - uv pip compile requirements.in --python-version 3.11 --python-platform x86_64-manylinux_2_36 --index-strategy unsafe-best-match --upgrade --no-header --no-annotate --output-file requirements.txt

  imagerun:
    desc: Run baked image with UI and API on PORT
    cmds:
      - cmd /c docker rm -f {{.CONTAINER}} >nul 2>&1
      - cmd /c docker rm -f {{.CONTAINER_LOCAL}} >nul 2>&1
      - task: cache-init
      - cmd /c docker run -d --name {{.CONTAINER}} -p {{.PORT}}:7861 --gpus all -e CUDA_VISIBLE_DEVICES={{.GPU_DEVICE}} -e OMNIVOICE_DEVICE={{.APP_DEVICE}} -e PORT=7861 -e HF_TOKEN={{.HF_TOKEN}} -e HF_HUB_OFFLINE={{.HF_HUB_OFFLINE}} -e TRANSFORMERS_OFFLINE={{.TRANSFORMERS_OFFLINE}} -v {{.HF_CACHE_VOLUME}}:/app/.cache/huggingface {{.IMAGE}}
      - cmd /c docker ps --filter "name={{.CONTAINER}}"

  imagerun-tiny:
    desc: Run tiny image online on first use, using the persistent Hugging Face cache volume
    cmds:
      - cmd /c docker rm -f {{.CONTAINER}} >nul 2>&1
      - cmd /c docker rm -f {{.CONTAINER_LOCAL}} >nul 2>&1
      - cmd /c docker volume create {{.HF_CACHE_VOLUME}} >nul
      - cmd /c docker run -d --name {{.CONTAINER}} -p {{.PORT}}:7861 --gpus all -e CUDA_VISIBLE_DEVICES={{.GPU_DEVICE}} -e OMNIVOICE_DEVICE={{.APP_DEVICE}} -e PORT=7861 -e HF_TOKEN={{.HF_TOKEN}} -e HF_HUB_OFFLINE=0 -e TRANSFORMERS_OFFLINE=0 -v {{.HF_CACHE_VOLUME}}:/app/.cache/huggingface {{.TINY_IMAGE}}
      - cmd /c docker ps --filter "name={{.CONTAINER}}"

  localrun:
    desc: Run baked image with local omnivoice/ bind-mounted for hot-swapping
    cmds:
      - cmd /c docker rm -f {{.CONTAINER}} >nul 2>&1
      - cmd /c docker rm -f {{.CONTAINER_LOCAL}} >nul 2>&1
      - task: cache-init
      - cmd /c docker run -d --name {{.CONTAINER_LOCAL}} -p {{.PORT}}:7861 --gpus all -e CUDA_VISIBLE_DEVICES={{.GPU_DEVICE}} -e OMNIVOICE_DEVICE={{.APP_DEVICE}} -e PORT=7861 -e UVICORN_RELOAD=1 -e HF_TOKEN={{.HF_TOKEN}} -e HF_HUB_OFFLINE={{.HF_HUB_OFFLINE}} -e TRANSFORMERS_OFFLINE={{.TRANSFORMERS_OFFLINE}} -v {{.HF_CACHE_VOLUME}}:/app/.cache/huggingface -v "{{.TASKFILE_DIR}}\omnivoice:/app/omnivoice" {{.IMAGE}}
      - cmd /c docker ps --filter "name={{.CONTAINER_LOCAL}}"

  localrun-tiny:
    desc: Run tiny image with local omnivoice/ bind-mounted, online on first use
    cmds:
      - cmd /c docker rm -f {{.CONTAINER}} >nul 2>&1
      - cmd /c docker rm -f {{.CONTAINER_LOCAL}} >nul 2>&1
      - cmd /c docker volume create {{.HF_CACHE_VOLUME}} >nul
      - cmd /c docker run -d --name {{.CONTAINER_LOCAL}} -p {{.PORT}}:7861 --gpus all -e CUDA_VISIBLE_DEVICES={{.GPU_DEVICE}} -e OMNIVOICE_DEVICE={{.APP_DEVICE}} -e PORT=7861 -e UVICORN_RELOAD=1 -e HF_TOKEN={{.HF_TOKEN}} -e HF_HUB_OFFLINE=0 -e TRANSFORMERS_OFFLINE=0 -v {{.HF_CACHE_VOLUME}}:/app/.cache/huggingface -v "{{.TASKFILE_DIR}}\omnivoice:/app/omnivoice" {{.TINY_IMAGE}}
      - cmd /c docker ps --filter "name={{.CONTAINER_LOCAL}}"

  imageweb:
    desc: Open default browser to local OmniVoiceTTS UI
    cmds:
      - cmd /c start "" "http://localhost:{{.PORT}}"

  localps:
    desc: Show likely local agent/dev app processes
    cmds:
      - powershell -NoProfile -ExecutionPolicy Bypass -File scripts/local-processes.ps1 -Mode scan -RepoPath "{{.TASKFILE_DIR}}"

  localports:
    desc: Show likely local agent/dev app processes plus listening ports
    cmds:
      - powershell -NoProfile -ExecutionPolicy Bypass -File scripts/local-processes.ps1 -Mode scan -RepoPath "{{.TASKFILE_DIR}}" -IncludePorts

  localclean:
    desc: Stop Python/Node/dev processes whose command line references this repo
    cmds:
      - powershell -NoProfile -ExecutionPolicy Bypass -File scripts/local-processes.ps1 -Mode clean -RepoPath "{{.TASKFILE_DIR}}"

  appinject:
    desc: Inject local omnivoice/app.py into the running baked container and restart it
    cmds:
      - cmd /c docker cp "{{.TASKFILE_DIR}}\omnivoice\app.py" {{.CONTAINER}}:/app/omnivoice/app.py
      - cmd /c docker restart {{.CONTAINER}}
      - cmd /c docker ps --filter "name={{.CONTAINER}}"

  imageapi:
    desc: Call API, play generated temp audio, then remove it
    cmds:
      - 'cmd /c curl.exe -sS --retry 60 --retry-delay 2 --retry-connrefused http://localhost:{{.PORT}}/tts/ping >nul'
      - 'cmd /c curl.exe -sS -X POST http://localhost:{{.PORT}}/tts/generate -H "Content-Type: application/json" -d "{\"text\":\"{{.API_TEXT}}\",\"voice\":\"{{.API_VOICE}}\",\"language\":\"{{.API_LANGUAGE}}\",\"speed\":1.0,\"device\":\"{{.APP_DEVICE}}\",\"output_format\":\"{{.API_FORMAT}}\"}" --output {{.TEMP_AUDIO}}.{{.API_FORMAT}}'
      - 'cmd /c if not exist {{.TEMP_AUDIO}}.{{.API_FORMAT}} exit /b 1'
      - 'cmd /c python -c "from pathlib import Path; p=Path(r''{{.TEMP_AUDIO}}.{{.API_FORMAT}}''); assert p.exists() and p.stat().st_size > 0, p"'
      - 'powershell -NoProfile -ExecutionPolicy Bypass -File scripts/play-audio.ps1 -Path "{{.TEMP_AUDIO}}.{{.API_FORMAT}}"'
      - 'cmd /c del /f /q {{.TEMP_AUDIO}}.{{.API_FORMAT}}'

  imageapi-format:
    desc: Call API for API_FORMAT and verify the output file is non-empty
    cmds:
      - 'cmd /c curl.exe -sS --retry 60 --retry-delay 2 --retry-connrefused http://localhost:{{.PORT}}/tts/ping >nul'
      - 'cmd /c curl.exe -sS -X POST http://localhost:{{.PORT}}/tts/generate -H "Content-Type: application/json" -d "{\"text\":\"{{.API_TEXT}}\",\"voice\":\"{{.API_VOICE}}\",\"language\":\"{{.API_LANGUAGE}}\",\"speed\":1.0,\"device\":\"{{.APP_DEVICE}}\",\"output_format\":\"{{.API_FORMAT}}\"}" --output {{.TEMP_AUDIO}}.{{.API_FORMAT}}'
      - 'cmd /c if not exist {{.TEMP_AUDIO}}.{{.API_FORMAT}} exit /b 1'
      - 'cmd /c python -c "from pathlib import Path; p=Path(r''{{.TEMP_AUDIO}}.{{.API_FORMAT}}''); assert p.exists() and p.stat().st_size > 0, p"'
      - 'cmd /c del /f /q {{.TEMP_AUDIO}}.{{.API_FORMAT}}'

  imagestop:
    desc: Stop running OmniVoiceTTS containers
    cmds:
      - cmd: cmd /c docker stop {{.CONTAINER}} >nul 2>&1
        ignore_error: true
      - cmd: cmd /c docker stop {{.CONTAINER_LOCAL}} >nul 2>&1
        ignore_error: true

  logs:
    desc: Show logs from active OmniVoiceTTS container
    cmds:
      - cmd /c docker logs --tail 200 {{.CONTAINER_LOCAL}} 2>nul || docker logs --tail 200 {{.CONTAINER}}

  nuke:
    desc: Remove OmniVoiceTTS local containers, images, and Hugging Face cache volume
    cmds:
      - cmd /c docker rm -f {{.CONTAINER}} >nul 2>&1
      - cmd /c docker rm -f {{.CONTAINER_LOCAL}} >nul 2>&1
      - cmd /c docker rmi -f {{.IMAGE}} >nul 2>&1
      - cmd /c docker rmi -f {{.TINY_IMAGE}} >nul 2>&1
      - cmd /c docker volume rm -f {{.HF_CACHE_VOLUME}} >nul 2>&1
